import React from 'react';
import Countdown from 'react-countdown';

const Time = () => {
    return (
        <div className='bg-red-600 text-white'>
          
        <Countdown  date={Date.now() +  dudationmonth }>
      </Countdown>
        </div>
    );
};

export default Time;