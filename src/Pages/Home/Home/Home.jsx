import React, { useContext } from 'react';
import HeroSection from '../HeroSection/HeroSection';

const Home = () => {
    return (
        <div className='mx-2'>
            <HeroSection/>
        </div>
    );
};

export default Home;